grid

function create(w, h) : return a new (static) grid

full 2D  Vs  3D engine but used in 2D for now...

2 different engines, depending on the config (WarDrums Vs IPhone, eee)
	=> create 2D engine first

2D engine :
   PC : SDL

WEB : HTML / CSS

